Tipovi:
number, string, funkcije, klase, interfejsi, polja, mape

Funkcije:

fun number f(string a, string b);

fun number f(number a, fun string g(string a));

Polja:

var array number arr;
var array array matrix;

var map number m;

var map array number table;

typedef map number numbers_map;

typedef map array number number_arrays_map;

Klase:

class s{
    var string a;
    var number b;
    fun number f(string a, string b);
    var fun number d(number n);

    constructor(cref string a, cref string b, fun d(number n)){
	this.a = a;
	this.b = b;
	this.d = d;
    }

    destructor(){

    }
}

intefrace i{
    var number b;
    fun number f(string a, string b);
    fun d(number n);
}

Deklaracija varijable:

var number n;
var number m(42);
var string str;
var string ime("Vrane");

var fun number sum[n](number x, number y){
    return x + y + n;

}

var s obj("a", "b", fun(number n){
    return n*n;
});
